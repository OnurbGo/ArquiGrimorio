import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
    padding: 16,
  },
  title: {
    fontSize: 72,
    fontWeight: "bold",
    color: "#d32f2f",
  },
  message: {
    fontSize: 20,
    color: "#333",
    marginBottom: 24,
  },
});
